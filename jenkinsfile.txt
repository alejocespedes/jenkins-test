pipeline {
    agent any
    parameters{
         string(name: 'PERSON', defaultValue: , description: 'Who should I say hello to?')
    }
    stages {
        stage('build') {
            steps {
                echo "hola ${params.PERSON}"
            }
        }
    }
}
