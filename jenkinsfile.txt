pipeline {
    agent any
    parameters {
         string(name: 'PERSON', defaultValue: '', description: 'Who should I say hello to?')
         choice(name:'seleccion', choices ['1','2'], description:'select')
         text(name: 'NAME', defaulValue:'', descrition:'Name', description 'texto')
    }
    stages {
        stage('build') {
            steps {
                echo "hola ${params.NAME}"
            }
        }
    }
}
